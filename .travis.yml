language: python
cache:
  - pip
matrix:
    allow_failures:
        - os: linux
    include:
        - os: linux
          python: 2.7
          env: TOXENV=py27
        - os: osx
          language: generic
          env: TOXENV=py27
install:
  - ./.travis/install.sh

script:
  - py.test -v test
